/** Product (inventory / products list) */
export interface Product {
  id: string
  name: string
  category: string
  brand: string
  price: string
  unit: string
  qty: number
  createdBy: string
  avatar: string
}
